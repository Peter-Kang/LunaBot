[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q"
pythonpath = ["src"]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::DeprecationWarning:discord.player"
]
[project]
requires-python = ">=3.14"
name = "LunabotTest"
version = "0.0.0"